<Your Task>
Write a short CLUSTER AGGREGATE caption for the WHOLE cluster (not one medoid).
Input is a numeric digest for gpl-odd oncoming scenarios on an urban/suburban
mapped network — not highway free-flow.
</Your Task>

<How to read the numbers>
- collision_rate is the primary risk signal.
- TTC here is a Payload polygon look-ahead KPI (mostly longitudinal). The same
  absolute TTC does NOT mean the same thing on highway vs urban roads, and a
  lateral pass-by / near-miss can show a "small" TTC while still being a
  successful yield. Prefer collide-vs-survive TTC splits and cross-cluster
  comparison over absolute claims like "3 s is safe".
- Do not invent MTTC, joint lateral–longitudinal predictors, or other metrics
  not present in the digest. If longitudinal TTC understates lateral risk,
  say that as a limitation — do not fabricate numbers.
- IC OncomingSpeed / OncomingStartDelay: descriptive ranges only — not safe/risky
  bounds.
- Embedding spread / outliers: outcome homogeneity vs geometric spread.
</How to read the numbers>

<Mandatory Workflow & Chain Of Thought>
1. Outcome mix: collision_rate, n_trials; collide vs survive TTC if both exist.
2. Conflict tightness: within this ODD, is the survivor TTC distribution tight or
   wide? Compare p10 vs p90 — do not import highway headway lore.
3. IC digests: restate mean/p10/p90; descriptive only.
4. Consistency: does embedding spread contradict outcome homogeneity?
</Mandatory Workflow & Chain Of Thought>

<Numeric digest (authoritative)>
{numeric_digest}
</Numeric digest>

<Output>
After CoT, ONE YAML fence:

```yaml
cluster_id: <int>
label: "<2-5 words>"
risk_level: "low" | "medium" | "high"
confidence: "high" | "medium" | "low"
caption: |
  3-5 short paragraphs. Lead with collision_rate. Cite TTC/IC only with the
  caveats above. Blank line between paragraphs.
consistency_note: |
  Embedding spread vs outcome homogeneity in one short note.
```
</Output>
