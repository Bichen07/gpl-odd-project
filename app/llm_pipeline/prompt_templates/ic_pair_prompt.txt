<Your Task>
INITIAL-CONDITION matched pair: two trials with nearly identical scenario inputs
(OncomingSpeed, OncomingStartDelay; param_dist ≤ caliper) from different embedding
clusters. Explain whether the cluster split is justified or over-fine.

IC params are sampled OpenSCENARIO inputs (Opposite start/target speed and start
delay) — NOT measured speed at peak conflict.
</Your Task>

<How to read>
- card_role=primary → outcomes differ → ask why behavior/outcome diverged.
- card_role=secondary → same outcome → ask if embedding over-split the ODD.
- Prefer conflict_metrics + context timeline over guessing from BEV alone.
- Synced BEVs: each image is ego±25m | ego±25m at the **same shared clock** t
  (left = cluster A, right = cluster B). Title event tokens match filenames
  (e.g. `0p5s_before_APPROACH_Opposite`). See common_sense BEV reading guide
  for arrows / trails / chips.
</How to read>

<Mandatory Workflow & Chain Of Thought>
1. Pair facts: param_dist, card_role, both trial folders, outcomes, IC params.
2. Left: peak_t / brake_t / relevance_t / d_min / ttc_min; cite context beats.
3. Right: same.
4. Earliest decisive divergence (timing, brake, gap, heading).
5. separation_call: justified | over_fine | inconclusive — with one-line reason.
6. Optional testable hypothesis. No global ODD bounds from one pair.
</Mandatory Workflow & Chain Of Thought>

<Pair facts>
{pair_facts}
</Pair facts>

<Left trial>
{left_block}
</Left trial>

<Right trial>
{right_block}
</Right trial>

<Left context (truncated)>
{left_context}
</Left context>

<Right context (truncated)>
{right_context}
</Right context>

<Output>
After CoT, ONE YAML fence. Do NOT invent conflict_metrics (pipeline injects GT).

```yaml
clusters: [<a>, <b>]
param_dist: <float>
card_role: "primary" | "secondary"
param_names: ["OncomingSpeed", "OncomingStartDelay"]
left:
  trial: "trial_<esmini_index>"
  trial_index: <int>
  payload_trial_id: "<payload id>"
  cluster: <int>
  outcome: "collision" | "near_miss" | "safe"
  params: {}
right:
  trial: "trial_<esmini_index>"
  trial_index: <int>
  payload_trial_id: "<payload id>"
  cluster: <int>
  outcome: "collision" | "near_miss" | "safe"
  params: {}
delta: {}
contrast_explanation: |
  Why matched ICs diverged (or look alike). Cite trial_<index>. Blank lines
  between paragraphs.
separation_call: "justified" | "over_fine" | "inconclusive"
separation_reason: |
  One short note.
hypothesis: |
  Testable follow-up, or empty.
```
</Output>
