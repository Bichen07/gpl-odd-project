#ifndef _UTILS_DEFAULT_COLOR_H_
#define _UTILS_DEFAULT_COLOR_H_

#include <std_msgs/ColorRGBA.h>

namespace utils {

std_msgs::ColorRGBA White(const float alpha = 1.0f);
std_msgs::ColorRGBA Black(const float alpha = 1.0f);
std_msgs::ColorRGBA Gray(const float alpha = 1.0f);
std_msgs::ColorRGBA Red(const float alpha = 1.0f);
std_msgs::ColorRGBA Green(const float alpha = 1.0f);
std_msgs::ColorRGBA Blue(const float alpha = 1.0f);
std_msgs::ColorRGBA Cyan(const float alpha = 1.0f);
std_msgs::ColorRGBA Magenta(const float alpha = 1.0f);
std_msgs::ColorRGBA Yellow(const float alpha = 1.0f);
std_msgs::ColorRGBA Purple(const float alpha = 1.0f);
std_msgs::ColorRGBA Tomato(const float alpha = 1.0f);
std_msgs::ColorRGBA Lime(const float alpha = 1.0f);
std_msgs::ColorRGBA Brown(const float alpha = 1.0f);
std_msgs::ColorRGBA Teal(const float alpha = 1.0f);
std_msgs::ColorRGBA OrangeRed(const float alpha = 1.0f);
std_msgs::ColorRGBA DarkOrange(const float alpha = 1.0f);
std_msgs::ColorRGBA DarkRed(const float alpha = 1.0f);
std_msgs::ColorRGBA BlueViolet(const float alpha = 1.0f);

} // namespace utils {

#endif // #ifndef _UTILS_DEFAULT_COLOR_H_
