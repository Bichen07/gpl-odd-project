#ifndef _MAP_UTILS_H_
#define _MAP_UTILS_H_

#include <string>
#include <map_type.h>

namespace map {

DirectionId QueryDirectionId(const std::string &key);

} // namespace map {

#endif // #ifndef _MAP_UTILS_H_
