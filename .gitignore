.temp

# Large local datasets — do not push to GitHub (files exceed 100MB limit)
alldatasets/

# Python cache
__pycache__/
*.pyc
*.pyo

# BEV output images — generated locally, not committed
bev_output/**/*.jpg
bev_output/**/*.png
# (bev_output/.gitkeep IS committed to preserve the folder)
